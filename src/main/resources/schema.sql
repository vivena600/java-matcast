CREATE TABLE stats (
   id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
   city VARCHAR(30) NOT NULL,
   sunny_days INTEGER DEFAULT 0,
   cloudy_days INTEGER DEFAULT 0,
   rainy_days INTEGER DEFAULT 0,
   max_temp INTEGER,
   min_temp INTEGER
)